::system.terminate
	0 0 io! // simulator control register
;
:system.emit
    BEGIN
	 0x2000 io@ 
	 1 and 0 =
	 core.invert
    UNTIL
    0x1000 io!
;

:system.emit.cr
    system.ascii.cr system.emit
    system.ascii.nl system.emit
;